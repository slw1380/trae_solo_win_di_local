# 鸿蒙（HarmonyOS）开发环境搭建指南

## 一、概述

HarmonyOS（鸿蒙操作系统）是华为开发的分布式操作系统，支持手机、平板、智能穿戴、智慧屏、车机等多种终端设备。本文档介绍如何搭建 HarmonyOS 应用开发环境。

> **适用版本**：HarmonyOS NEXT（HarmonyOS 5.0）及 HarmonyOS 4.x  
> **开发语言**：ArkTS（推荐）、JavaScript、C/C++

---

## 二、系统要求

### 2.1 硬件要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| 内存 | 8 GB | 16 GB 及以上 |
| 硬盘 | 100 GB 可用空间 | SSD，200 GB 及以上 |
| 分辨率 | 1280 × 800 | 1920 × 1080 及以上 |

### 2.2 操作系统要求

| 操作系统 | 版本要求 |
|---------|---------|
| Windows | Windows 10/11 64 位 |
| macOS | macOS 11 (Big Sur) 及以上 |
| Ubuntu | Ubuntu 20.04 及以上 |

---

## 三、安装 DevEco Studio

DevEco Studio 是华为官方提供的鸿蒙应用集成开发环境（IDE），基于 IntelliJ IDEA 社区版打造。

### 3.1 下载 DevEco Studio

1. 访问华为开发者官网：[https://developer.huawei.com/consumer/cn/deveco-studio](https://developer.huawei.com/consumer/cn/deveco-studio)
2. 选择对应操作系统的安装包下载：
   - Windows：`deveco-studio-x.x.x.xxx.exe`
   - macOS (Intel)：`deveco-studio-x.x.x.xxx.dmg`
   - macOS (Apple Silicon)：`deveco-studio-x.x.x.xxx-arm.dmg`
   - Linux：`deveco-studio-x.x.x.xxx.tar.gz`

### 3.2 Windows 安装步骤

1. 双击下载的 `.exe` 安装包
2. 选择安装路径（建议不要包含中文或空格）
3. 勾选以下选项：
   - [x] 创建桌面快捷方式
   - [x] 添加环境变量（可选）
4. 点击「安装」并等待完成
5. 安装完成后，启动 DevEco Studio

### 3.3 macOS 安装步骤

1. 打开下载的 `.dmg` 文件
2. 将 DevEco Studio 拖入 Applications 文件夹
3. 首次启动时，若提示「无法打开」，前往：
   **系统设置 → 隐私与安全性 → 安全性 → 仍要打开**
4. 启动后按向导完成初始化配置

### 3.4 Linux 安装步骤

```bash
# 1. 解压安装包
tar -zxvf deveco-studio-x.x.x.xxx.tar.gz -C /opt/

# 2. 进入目录
cd /opt/deveco-studio/bin

# 3. 启动 IDE
./deveco-studio.sh
```

---

## 四、配置开发环境

### 4.1 首次启动配置

1. **导入设置**：首次启动可选择「不导入设置」
2. **同意协议**：阅读并同意华为开发者协议
3. **选择安装类型**：
   - **标准安装**（推荐）：自动下载常用 SDK 和工具
   - **自定义安装**：手动选择需要的组件

### 4.2 安装 SDK

1. 打开 DevEco Studio
2. 进入 **Settings/Preferences → SDK → HarmonyOS**
3. 选择需要安装的 SDK 版本：
   - **HarmonyOS NEXT (5.0)** — 最新版本，仅支持 Stage 模型
   - **HarmonyOS 4.x** — 兼容版本，支持 FA 和 Stage 模型
4. 勾选对应版本的 **SDK**、**Toolchains**、**Emulator**
5. 点击「Apply」开始下载安装

### 4.3 配置 Node.js 环境

DevEco Studio 内置 Node.js，也可使用本地安装版本：

```bash
# 查看内置 Node.js 版本
node -v

# 如需使用本地 Node.js，在 Settings 中配置：
# Settings → Languages & Frameworks → Node.js
```

> **注意**：HarmonyOS 开发要求 Node.js 版本为 16.x 及以上。

### 4.4 配置 Ohpm（Open Harmony Package Manager）

Ohpm 是鸿蒙生态的包管理工具，类似 npm：

1. 进入 **Settings → SDK → HarmonyOS → Ohpm**
2. 选择内置 Ohpm 或配置本地路径
3. 配置代理（如需）：
   ```bash
   ohpm config set registry https://repo.harmonyos.com/npm/
   ohpm config set proxy http://your-proxy:port
   ```

---

## 五、配置模拟器/真机调试

### 5.1 使用远程模拟器

1. 点击工具栏的 **Device Manager** 图标
2. 选择 **Remote Emulator** → 登录华为账号
3. 选择需要的设备类型：
   - Phone（手机）
   - Tablet（平板）
   - Wearable（穿戴设备）
   - TV（智慧屏）
4. 点击「启动」即可运行应用

> **注意**：远程模拟器需要联网，每天有免费使用时长限制。

### 5.2 使用本地模拟器

1. 在 **Device Manager** 中选择 **Local Emulator**
2. 点击「创建」选择设备模板
3. 下载系统镜像（首次使用需下载）
4. 启动本地模拟器运行应用

### 5.3 真机调试

#### 5.3.1 注册华为开发者账号

1. 访问 [华为开发者联盟](https://developer.huawei.com/consumer/cn/)
2. 注册并实名认证开发者账号

#### 5.3.2 配置签名证书

1. 在 DevEco Studio 中，点击 **File → Project Structure**
2. 选择 **Signing Configs** → **自动签名**
3. 登录华为账号，IDE 会自动生成调试证书

#### 5.3.3 连接真机

1. 开启手机「开发者模式」：
   **设置 → 关于手机 → 版本号**（连续点击 7 次）
2. 开启 USB 调试：
   **设置 → 系统和更新 → 开发人员选项 → USB 调试**
3. 使用数据线连接电脑，授权调试
4. 在 DevEco Studio 的设备列表中选择真机运行

---

## 六、创建第一个鸿蒙项目

### 6.1 新建项目

1. 打开 DevEco Studio，点击 **Create Project**
2. 选择项目模板：
   - **Empty Ability**（空项目，推荐入门）
   - **Full Ability**（完整示例项目）
3. 配置项目信息：
   - **Project name**：项目名称
   - **Bundle name**：应用包名（如 `com.example.myapp`）
   - **Save location**：项目保存路径
   - **Compile SDK**：选择编译 SDK 版本
   - **Model**：选择 Stage Model（推荐）或 FA Model
   - **Language**：选择 ArkTS（推荐）或 JS
4. 点击「Finish」创建项目

### 6.2 项目结构说明

```
MyApplication/
├── AppScope/                    # 应用级配置
│   └── resources/               # 应用级资源
├── entry/                       # 入口模块（默认模块）
│   ├── src/
│   │   ├── main/
│   │   │   ├── ets/             # ArkTS 源码目录
│   │   │   │   ├── entryability/# Ability 业务逻辑
│   │   │   │   └── pages/       # 页面目录
│   │   │   ├── resources/       # 模块资源文件
│   │   │   └── module.json5     # 模块配置文件
│   └── build-profile.json5      # 构建配置
├── build-profile.json5          # 工程构建配置
├── hvigorfile.ts                # 构建脚本
├── oh-package.json5             # 依赖配置
└── oh_modules/                  # 依赖包目录
```

### 6.3 运行项目

1. 选择目标设备（模拟器或真机）
2. 点击工具栏的 **Run** 按钮（▶️）或按 `Shift + F10`
3. 等待编译完成，应用自动安装并启动

---

## 七、常见问题解决

### 7.1 安装问题

| 问题 | 解决方案 |
|------|---------|
| 安装包无法打开 | 检查系统版本是否满足要求，关闭杀毒软件重试 |
| SDK 下载失败 | 检查网络连接，配置代理或更换网络环境 |
| 磁盘空间不足 | 清理磁盘或更换安装路径，确保有 100GB 以上空间 |

### 7.2 编译问题

| 问题 | 解决方案 |
|------|---------|
| 编译报错 "SDK 未找到" | 检查 SDK 路径配置，重新安装对应版本 SDK |
| 依赖下载失败 | 检查 ohpm 配置，尝试切换 registry 或配置代理 |
| 内存不足编译失败 | 关闭其他程序，或增加 Node.js 内存限制：`--max-old-space-size=4096` |

### 7.3 模拟器问题

| 问题 | 解决方案 |
|------|---------|
| 模拟器启动失败 | 检查是否开启虚拟化（VT-x/AMD-V），BIOS 中启用 Intel VT-x 或 AMD SVM |
| 远程模拟器无法连接 | 检查网络，确认已登录华为账号 |
| 模拟器卡顿 | 降低模拟器分辨率，或增加电脑内存 |

### 7.4 真机调试问题

| 问题 | 解决方案 |
|------|---------|
| 无法识别设备 | 检查 USB 调试是否开启，更换数据线或 USB 接口 |
| 安装失败（签名错误） | 检查签名配置，重新生成调试证书 |
| 应用闪退 | 查看日志（Logcat），检查权限声明和代码逻辑 |

---

## 八、常用命令

```bash
# 安装依赖
ohpm install

# 构建项目
hvigor assemble

# 运行测试
hvigor test

# 清理构建产物
hvigor clean

# 查看 ohpm 配置
ohpm config list

# 搜索包
ohpm search <package-name>
```

---

## 九、学习资源

| 资源 | 链接 |
|------|------|
| 官方文档 | [https://developer.harmonyos.com](https://developer.harmonyos.com) |
| 开发者论坛 | [https://forum.developer.harmonyos.com](https://forum.developer.harmonyos.com) |
| Codelabs 教程 | DevEco Studio → Help → Codelabs |
| API 参考 | [https://developer.harmonyos.com/docs](https://developer.harmonyos.com/docs) |
| 示例代码 | [https://gitee.com/harmonyos_samples](https://gitee.com/harmonyos_samples) |

---

## 十、附录

### 10.1 环境变量配置（可选）

**Windows**：
```
系统属性 → 高级 → 环境变量 → Path → 添加 DevEco Studio 和 SDK 路径
```

**macOS/Linux**：
```bash
# 编辑 ~/.bashrc 或 ~/.zshrc
export DEVECO_HOME=/Applications/DevEco-Studio.app/Contents
export PATH=$PATH:$DEVECO_HOME/bin
```

### 10.2 版本对照表

| DevEco Studio | HarmonyOS SDK | 说明 |
|--------------|---------------|------|
| 4.0 Release | HarmonyOS 4.0 | 稳定版本 |
| 5.0 Release | HarmonyOS NEXT | 最新版本，API 12+ |

---

> **提示**：建议定期更新 DevEco Studio 和 SDK 到最新版本，以获取最新功能和修复。
